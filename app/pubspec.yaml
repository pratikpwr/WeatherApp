name: app
description: A whether app for farmers

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^2.1.0
  flutter_bloc: ^7.3.0

  provider: ^6.0.1

  intl: ^0.17.0
  geolocator: ^7.7.0

  # Local
  shared:
    path: '../shared'
  api:
    path: '../api'

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/3d/
    - assets/3d-resized/